<table class="table table-hover">
    <thead>
      <tr>
        <th>Date</th>
        <th>Goal</th>
        <th>Category</th>
        <th>Tags</th>
        <th>Due Date</th>
      </tr>
    </thead>
    <tbody id="goals_table">
      <% goals.forEach((goal) => {%>
      <tr>
        <td><%= goal.time_submitted %></td>
        <td><%= goal.goal %></td>
        <td><%= goal.category %></td>
        <td><%= goal.tags %></td>
        <td><%= goal.due_date %></td>
        <td>
        <form method="POST" action="/complete/<%= goal.goal_id %>">
          <button>
            ✅
          </button>
        </form>
        </td>
        <td>
        <form method="POST" action="/delete/<%= goal.goal_id %>">
          <button>
            DELETE
          </button>
        </form>
        </td>
      </tr>
      <% }) %>
    </tbody>
  </table>
